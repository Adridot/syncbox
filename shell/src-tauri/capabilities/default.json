{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core defaults plus opening external URLs in the system browser (purchase links, Spotify authorize, logs folder) and native file/folder pickers (paths settings).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "opener:default",
    "dialog:default",
    {
      "identifier": "opener:allow-open-url",
      "allow": [{ "url": "spotify:*" }, { "url": "https://*" }]
    }
  ]
}
