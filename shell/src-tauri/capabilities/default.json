{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core defaults plus opening external URLs in the system browser (purchase links, Spotify authorize, logs folder).",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
