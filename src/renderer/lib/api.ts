// Barrel: API types live in ./api/types, the HTTP client in ./api/client.
// Existing imports (`from "../lib/api"`) keep working unchanged.
export * from "./api/types";
export * from "./api/client";
